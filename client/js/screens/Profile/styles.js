import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  numbers: {
    paddingTop: 2,
    fontSize: 14,
    fontWeight: 'bold',
    color: '#13443C',
  },
  textFollow: {
    color: '#1e6a62',
  },
})

export default styles
