import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  textTitle: {
    fontSize: 25,
    color: '#13443c',
    fontWeight: 'bold',
    paddingTop: 6,
  },
})

export default styles
